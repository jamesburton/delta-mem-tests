{
  "version": "1.0",
  "timestamp": "2026-05-29T00:00:00Z",
  "context": "research",
  "project": "delta-mem-tests",
  "status": "paused",
  "last_commit": "9cd4ecc",
  "current_milestone": "Tier 1 reproduction + KV-quantisation x delta-mem experiment ladder + OSCAR port",
  "what_we_proved": [
    {"experiment": "bf16 baseline (conv-0 full + conv-1+8)", "result": "ratio 0.99x weighted across 389 q vs paper 1.20x", "verdict": "OUT_OF_BAND/REGRESSION; likely adapter mismatch"},
    {"experiment": "delta-only (conv-0, 152 q)", "result": "delta_only 0.1064 vs truncated_base 0.0591 = 1.80x lift; temporal questions 20x lift", "verdict": "delta-mem captures real compressed memory; ~30% of full-attention quality"},
    {"experiment": "TurboQuant 4-bit (conv-0, 10 q)", "result": "base 0.3344 / delta 0.3342 vs bf16 0.2680/0.2680 same slice", "verdict": "quality preserved at 4-bit; ~73% memory saving; pure-python codebook is ~50 min/q vs bf16's 5 min/q"},
    {"experiment": "HQQ 2-bit (conv-0, 10 q)", "result": "all scores 0.0000, outputs gibberish", "verdict": "naive 2-bit destroys attention; empirically validates need for OSCAR's spectral-rotation preconditioning"},
    {"experiment": "OSCAR rotation correctness (apply_rotations on Qwen3-4B-Instruct-2507)", "result": "rotation-only output matches baseline byte-for-byte on the short smoke prompt; INT2 path same", "verdict": "rotation math is correct; Thinking-2507 rotations DO NOT obviously break Instruct-2507; INT2 middle region not yet exercised (prompt under sink_tokens=64)"}
  ],
  "infrastructure_state": {
    "repo": "E:\\Development\\delta-mem-tests (git, main branch, clean at 9cd4ecc)",
    "vendored_submodule_delta_mem": "delta-Mem at pinned commit 98dc679572ef77d77b97485bf2f2b2aa810b74ba (unmodified on disk)",
    "vendored_submodule_oscar": "third_party/oscar-transformers at 55760e2 (https://github.com/jamesburton/oscar-transformers, MIT)",
    "venv": ".venv via uv; CUDA 12.1 torch 2.5.1; turboquant 0.2.0 + hqq 0.2.8.post1 + optimum-quanto 0.2.5 + oscar-transformers 0.0.2 (editable from submodule) all installed",
    "windows_quirks": "MSVC env via env/vsenv.ps1; CUDA 13.1 needs -allow-unsupported-compiler for VS 2025; optimum-quanto is Linux-only (gemm_cuda.cu uses GCC __asm__)",
    "hardware": "Native Windows 11, internal RTX 3060 12 GB (T5500 workstation)",
    "rotations_cached_at": "data/oscar/rotations/_hf_cache/Qwen3-4B-Thinking-2507/seq20000_prompt83_group128/ (gitignored; re-fetch from huggingface.co/Zhongzhu/OSCAR-RotationZoo)"
  },
  "in_process_patches": [
    "_chunked_eval_runner.py: chunked-prefill of build_teacher_forced_snapshot (history_replay mode, inert in official_prompt)",
    "_chunked_eval_runner.py: OOM-class normalisation wrapping _generate_prompt_chunk (inert in official_prompt)",
    "_chunked_eval_runner.py: chunked prefill of generate_official_full_history_answer (replaces monolithic 17.6k-token model.generate that OOMs at 37 GB)",
    "_chunked_eval_runner.py: per-conversation KV-cache reuse via Cache.crop(history_len) — disabled when KV_CACHE_BACKEND != bf16",
    "_chunked_eval_runner.py: KV_CACHE_BACKEND env var (bf16|turboquant|quanto|hqq|oscar) + KV_CACHE_BITS",
    "_chunked_eval_runner.py: oscar branch loads rotations + calls apply_rotations once per process (guarded by _OSCAR_ROTATIONS_BAKED flag)"
  ],
  "oscar_module_state": {
    "repo_url": "https://github.com/jamesburton/oscar-transformers",
    "license": "MIT (port); attribution to FutureMLS-Lab/OSCAR (algorithm, MIT) and Zhongzhu/OSCAR-RotationZoo (calibrated rotations, Apache-2.0) in NOTICE",
    "commits": [
      {"sha": "4d4b57a", "summary": "Initial skeleton: rotation, quantize, cache modules + 12 unit tests"},
      {"sha": "55760e2", "summary": "Replace bake_rotations with apply_rotations to handle Qwen3 QK Norm + RoPE"}
    ],
    "tests": "13 unit tests, all pass on transformers 5.9.0. The key one is test_apply_rotations_preserves_qwen3_block_output, which exercises a real Qwen3Attention block with q_norm/k_norm/RoPE/GQA and verifies orthogonal invariance to atol=1e-4 at fp32.",
    "smoke_result": "All three of baseline / rotation-only / OSCAR-INT2 produce 'The capital of France is Paris.' Caveat: ~30-token prompt is below sink_tokens=64 so the INT2 middle region was never populated; rotation math is validated, INT2 middle-region quality is not."
  },
  "next_planned_experiment": {
    "name": "OSCAR conv-0/10q",
    "rationale": "Same slice as TQ4 (Appendix B) and HQQ2 (Appendix C) baselines; produces the cross-method comparison that justified the OSCAR port in the first place.",
    "preconditions_met": ["OSCARCache + apply_rotations built and unit-tested", "KV_CACHE_BACKEND=oscar wired into runner", "Thinking-2507 rotations downloaded and shape-confirmed for Instruct-2507", "Short-prompt smoke produces coherent output"],
    "preconditions_open": ["Long-prompt smoke (~1k tokens) to exercise INT2 middle region before the 4-hour LoCoMo run — optional but cheap insurance"],
    "command_sketch": "PowerShell: $env:KV_CACHE_BACKEND='oscar'; $env:OSCAR_K_ROTATION_PATH='data/oscar/rotations/_hf_cache/.../k_rotation_qqt_r_h_pbr.pt'; $env:OSCAR_V_ROTATION_PATH='.../v_rotation_sst_r_h_pbr.pt'; then the standard locomo_eval invocation for conv-0 first 10 q with both branches",
    "contingency": "If conv-0/10q quality regresses badly under OSCAR, the Thinking rotations did not transfer to Instruct. Plan B: run the 3-phase OSCAR calibration on Instruct (dump Q/K/V activations, run compute_kv_rotation.py, save new rotations). This step is expensive and Windows-unfriendly because upstream uses sglang for the dump phase; may need WSL or a Linux box."
  },
  "blockers": [
    {"description": "OSCAR ships only as SGLang patches, not a transformers library. Port required.", "type": "engineering", "status": "RESOLVED — see third_party/oscar-transformers."},
    {"description": "RotationZoo on HF has Qwen3-4B-Thinking-2507 but not Qwen3-4B-Instruct-2507. We use the Instruct variant.", "type": "data", "status": "DEFERRED — using Thinking rotations as a transfer-test on Instruct. If conv-0/10q quality regresses, fall back to running our own calibration."},
    {"description": "optimum-quanto's CUDA extension uses GCC __asm__ inline assembly; doesn't compile on MSVC.", "type": "technical", "status": "ACCEPTED — skip optimum-quanto on Windows; HQQ remains available."},
    {"description": "Qwen3's QK Norm + RoPE break naive weight-baking of rotations.", "type": "math", "status": "RESOLVED — replaced bake_rotations with attention-forward monkey-patch apply_rotations. See submodule commit 55760e2."}
  ],
  "human_actions_pending": [
    {"action": "Decide whether to run a longer-prompt smoke (~1k tokens) before the LoCoMo eval, or skip straight to conv-0/10q", "context": "User picked 'pause cleanly' at the natural stopping point; either choice is reasonable in a fresh session", "blocking": false}
  ],
  "decisions_made_in_this_session": [
    {"decision": "Spin out OSCAR port as a new public repo (jamesburton/oscar-transformers, MIT) and vendor as submodule", "rationale": "Reusable artefact; doesn't entangle with delta-mem-tests lifecycle"},
    {"decision": "Use the weight-baking design FIRST (later corrected)", "rationale": "Initial preference (matched OSCAR's design intent in sglang); the failure case validated against a stub block. Lesson: always test against the real attention module, not a stub."},
    {"decision": "Pivot to attention-forward monkey-patching after empirical failure on the real model", "rationale": "QK Norm γ + RoPE both non-commutative with rotation. The patched-forward approach is mathematically correct and validated by the new end-to-end Qwen3Attention test."},
    {"decision": "Try Thinking-2507 rotations on Instruct-2507 first as a transfer test", "rationale": "Rotations are calibrated offline and somewhat task-agnostic; same architecture; cheaper than the 3-phase calibration on a Windows host. Short-prompt smoke gave the early green light."}
  ],
  "uncommitted_files": [],
  "next_action": "Either (A) run a ~1k-token smoke to exercise INT2 middle region, or (B) kick off conv-0/10q OSCAR run. See .continue-here.md for the exact PowerShell incantation."
}
